let x := 1
let y := x + -1
let a := x{+: y}{-}

let obj := [
  on {foo} 1
  on {bar: x} x + 2
  on {a: a b: var b} a + b
]

var v := 123
set v := 456
obj{a: 1 b: var v}

let obj := [
  on {do var let on}
    return
  on {bar: x}
    return x
]