export let Bool := [
  on {true} [
    on {true: t false: f}
      t
    on {!}
      Bool{false}
  ]
  on {false} [
    on {true: t false: f}
      f
    on {!}
      Bool{true}
  ]
]

export let [_true_ _false_] := Bool
